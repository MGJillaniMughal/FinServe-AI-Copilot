# --- Core (required) ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
python-multipart==0.0.9

# --- Optional: real LLM mode (auto-detected when OPENAI_API_KEY is set) ---
openai==1.30.1

# --- Optional: PDF / DOCX ingestion ---
pypdf==4.2.0
python-docx==1.1.2

# --- Optional: managed vector DB (TF-IDF store works without these) ---
chromadb==0.5.0
faiss-cpu==1.8.0
